#ifndef FUNC
#define FUNC

int add(int x, int y);

int validateFileName(char filename);
int readFile(char filename);
int drawMaze(char maze);
int checkInput();
int checkValidMove();
int movePlayer();

#endif